import { timelineImageOne } from "../images/ImgAssets";

const cards = [
  {
    id: 1,
    images: timelineImageOne, // Make sure this is an array of strings (URLs)
    content: 'Some content for card 1',
  },
  // other cards...
];

export default cards;
